<% if (typeof error != 'undefined' && error.length > 0) { %>
<div class="alert alert-danger col-md-push-3 col-md-6 col-xs-12">
    <ul>
        <% error.forEach(function(item){ %>
        <li><%= item %></li>
        <% }); %>
    </ul>
</div>
<% } %>